apiVersion: v1
kind: PersistentVolume
metadata:
  name: task-pv-volume
  namespace: datacubepy
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 10Gi
  accessModes:
    - ReadWriteMany
  hostPath:
    path: "/mnt/data"
